{
  "name": "fastandspinerr",
  "version": "1.0.0",
  "description": "Modified package.json to fix deployment issues",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install": "npm install --no-package-lock",
    "preinstall": "mkdir -p ./node_modules/youtube-dl-exec/bin && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o ./node_modules/youtube-dl-exec/bin/yt-dlp && chmod +x ./node_modules/youtube-dl-exec/bin/yt-dlp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "youtube-dl-exec": "^2.4.6"
  }
}